<UserControl x:Class="Cast.Desktop.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="600">
    <Grid Margin="24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{DynamicResource SettingsTitle}" FontSize="20" FontWeight="SemiBold"
                   Foreground="{DynamicResource FgPrimaryBrush}" Margin="0,0,0,16"/>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}">
            <StackPanel>
                <TextBlock Text="{DynamicResource SettingsOverlayHotkey}" Foreground="{DynamicResource FgSecondaryBrush}"
                           Margin="0,0,0,6"/>
                <StackPanel Orientation="Horizontal">
                    <!-- Кнопка перехвата клавиши: по нажатию ждёт следующую клавишу -->
                    <Button x:Name="CaptureButton" MinWidth="160" Padding="12,6"
                            Style="{StaticResource SecondaryButton}"
                            Click="OnCaptureClick" PreviewKeyDown="OnCaptureKeyDown"/>
                    <TextBlock Text="{DynamicResource SettingsHotkeyHint}" Margin="12,0,0,0"
                               VerticalAlignment="Center" Foreground="{DynamicResource FgSecondaryBrush}" FontSize="11"
                               TextWrapping="Wrap" MaxWidth="320"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>